dist/
coverage/
playwright-report/
test-results/

# Generated by `uv run python -m hive_core.snapshot`. It is written compact on
# purpose and CI checks it byte-for-byte against a fresh run, so reformatting it
# here would put the formatter and the generator permanently at odds.
public/demo/
dist/
