# JSON Schema Boundary

Use this directory only for transport-neutral schemas that genuinely need to be shared outside the Python API. Avoid duplicating Pydantic models without an explicit compatibility reason.
