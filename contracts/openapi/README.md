# OpenAPI Contract Boundary

The core service will publish one versioned `/api/v1` contract here. Generate the console client from the approved description or maintain a verified compatible client; do not hand-copy untracked response shapes.

No route or schema is defined until the domain model is implemented and tested.
