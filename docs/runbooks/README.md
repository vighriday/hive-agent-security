# Operational Runbooks

Runbooks are written only for behaviours that exist and can be tested. Each runbook must state its trigger, risk, preconditions, exact safe steps, verification, rollback, and owner.

Future candidates include replay recovery, ingest degradation, simulated-control failure, schema migration, and release rollback.
