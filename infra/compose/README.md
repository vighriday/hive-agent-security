# Local Compose Plan

Do not add a Compose stack until the single-process core and console prove P0. The eventual compose file must use local-only defaults, explicit named volumes, health checks, and no open public ports by default.
