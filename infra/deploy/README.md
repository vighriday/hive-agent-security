# Deployment Plan

Deployment definitions are deliberately absent until a deployment owner, threat model, data-retention policy, identity provider, and secret-management solution are selected. The hackathon deliverable must remain runnable locally without this directory.
