# HIVE Observability Plan

This directory will contain configuration for HIVE's own health telemetry only: ingest lag, replay determinism failures, detector errors, control-verification failures, and API latency. It is not a store for customer prompts, raw traces, or the security evidence ledger.
