# Policy Distribution Plan

HIVE manifests and explicit predicates are the first policy source. Add OPA/Rego assets here only when policy ownership/distribution is genuinely required. Do not duplicate security semantics across a manifest, Rego, and UI without contract tests.
