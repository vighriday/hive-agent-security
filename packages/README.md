# Shared Packages Boundary

Do not create a shared package pre-emptively. Add one here only when at least two consumers need the same stable, tested implementation. Likely future candidates are generated API types or an accessibility-tested design-token package—not policy, graph, or detection logic.
