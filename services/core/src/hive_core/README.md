# Core package seams

Each directory below is intentionally empty until a tested implementation task owns it. Maintain inward dependency flow: adapters and framework code depend on the domain; the domain depends on neither.
