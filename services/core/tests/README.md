# Core test map

No executable test is committed yet. Add tests with the first implemented behaviour and retain deterministic, fictional inputs only.

```text
unit/          pure domain/predicate/planner tests
integration/   ledger, graph projection, and simulated-control boundaries
contract/      API/schema compatibility tests
property/      generated ordering/idempotency/invariant tests
```
